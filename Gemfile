source "https://rubygems.org"

# Matches the exact gem versions GitHub Pages uses, so a local preview looks
# like the live site.
gem "github-pages", group: :jekyll_plugins

# Needed for `bundle exec jekyll serve` on Ruby 3.x
gem "webrick", "~> 1.8"
